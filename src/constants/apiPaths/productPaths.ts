const productPath = 'products';
const slugPath = 'slug';
const idPath = 'id';
const embeddingPath = 'get-product-by-embedding';

export const productPaths = {
    productPath,
    slugPath,
    idPath,
    embeddingPath
};
